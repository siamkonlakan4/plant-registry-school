export function confirmAction(message){return new Promise(resolve=>{const d=document.createElement('dialog');d.innerHTML=`<p></p><form method="dialog"><button value="cancel">ยกเลิก</button><button value="yes" class="danger">ยืนยัน</button></form>`;d.querySelector('p').textContent=message;document.body.append(d);d.addEventListener('close',()=>{resolve(d.returnValue==='yes');d.remove()});d.showModal()})}
