import {auth} from '../services/auth-service.js';import {protectedCall} from '../api/api-client.js';import {esc} from '../utils/sanitize.js';
export async function dashboard(el){if(!auth.user){location.hash='#/login';return}const rows=await protectedCall('/me/plants',{page:1,pageSize:100});el.innerHTML=`<section class="section"><div class="section-head"><div><div class="eyebrow">WORKSPACE</div><h1>ทะเบียนของฉัน</h1></div><a class="button" href="#/form">+ เพิ่มพรรณไม้</a></div><div class="stats">${[['รายการที่บันทึก',rows.length],['รอตรวจสอบ',rows.filter(p=>p.status==='pending').length],['ยังไม่มีรูป',rows.filter(p=>!p.cover_image_url).length]].map(([a,b])=>`<div class="stat"><strong>${b}</strong><span>${a}</span></div>`).join('')}</div><div class="panel"><h2>รายการล่าสุด</h2><div class="table-wrap"><table><thead><tr><th>รหัส</th><th>ชื่อพื้นเมือง</th><th>สถานะ</th><th>จัดการ</th></tr></thead><tbody>${rows.map(p=>`<tr><td>${esc(p.plant_code)}</td><td>${esc(p.common_name)}</td><td><span class="tag">${esc(p.status)}</span></td><td><a href="#/form/${p.plant_id}">แก้ไข</a></td></tr>`).join('')}</tbody></table></div></div><p><a href="#/labels">จัดการป้ายทะเบียน →</a></p></section>`}
